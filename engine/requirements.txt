# Engine + lichess-bot deps
torch==2.4.0
maia2
chess>=1.11
pyzstd
huggingface_hub>=0.24
fastapi>=0.110
uvicorn>=0.28
# lichess-bot itself is installed via git in the Dockerfile
# (it's not on PyPI; we clone its repo)
PyYAML>=6.0
backoff>=2.2
requests>=2.31
